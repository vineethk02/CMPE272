<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Hello World from SJSU-{{ server_id }}</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 4rem; }
    .card { border: 1px solid #e5e7eb; border-radius: 16px; padding: 2rem; box-shadow: 0 10px 25px rgba(0,0,0,0.05); }
    h1 { margin: 0 0 1rem 0; font-size: 2rem; }
    p { margin: 0.25rem 0; }
    code { background: #f3f4f6; padding: 0.2rem 0.4rem; border-radius: 6px; }
  </style>
</head>
<body>
  <div class="card">
    <h1>Hello World from SJSU-{{ server_id }}</h1>
    <p>This page is served by Nginx on port <code>8080</code>.</p>
    <p>Host: <code>{{ inventory_hostname }}</code> | Address: <code>{{ ansible_host }}</code></p>
  </div>
</body>
</html>
